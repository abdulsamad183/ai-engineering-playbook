# Interview SQL Cheat Sheet

- **JOIN:** INNER, LEFT (nulls for missing), FULL
- **GROUP BY** + HAVING for aggregates
- **Window:** `ROW_NUMBER`, `RANK`, moving avg
- **CTE:** readable multi-step queries
- Index columns in WHERE/JOIN

See [SQL Interviews](../domains/interview-preparation/sql-interviews.md).
