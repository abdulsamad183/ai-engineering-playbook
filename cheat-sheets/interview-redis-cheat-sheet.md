# Interview Redis Cheat Sheet

- Cache key: include `version` + tenant
- Rate limit: INCR + TTL or token bucket
- Session: hot conversation context
- `SET NX EX` distributed lock
- Eviction policy matters

See [Redis Interviews](../domains/interview-preparation/redis-interviews.md).
