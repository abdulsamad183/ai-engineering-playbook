# Interview Docker Cheat Sheet

- Multi-stage: builder → slim runtime
- Non-root USER in Dockerfile
- Secrets via env/orchestrator, not image
- Debug: `docker logs`, `exec -it bash`
- Bind `0.0.0.0` in containers

See [Docker Interviews](../domains/interview-preparation/docker-interviews.md).
