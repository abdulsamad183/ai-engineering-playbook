# Interview Python Cheat Sheet

- **GIL:** I/O → async; CPU → multiprocessing
- **async:** don't block event loop with CPU work
- **generators:** stream tokens/chunks lazily
- **decorators:** retry, timing, auth
- `@lru_cache` for pure function memoization

See [Python Interviews](../domains/interview-preparation/python-interviews.md).
