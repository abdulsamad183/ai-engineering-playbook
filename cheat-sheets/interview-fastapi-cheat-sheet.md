# Interview FastAPI Cheat Sheet

- ASGI: middleware → route → `Depends()` → handler
- Pydantic = validation + OpenAPI
- `async def` for I/O; executor for CPU
- `StreamingResponse` for SSE
- Test: `TestClient(app)`

See [FastAPI Interviews](../domains/interview-preparation/fastapi-interviews.md).
